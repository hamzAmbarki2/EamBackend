# Frontend environment variables
# Default proxy to Vite server configured in vite.config.ts
# Override if you need direct calls (e.g., when serving built assets)
# VITE_API_BASE_URL=http://localhost:8080/api